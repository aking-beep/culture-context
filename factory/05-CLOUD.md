# Cloud agents

In a cloud coding environment, the isolated container/clone is the station, so a nested worktree is unnecessary. Build/prove/review/ship rules remain the same. Commit evidence to the feature branch as it is captured because the container may be ephemeral. Reviewer context must still be separate from builder context.
