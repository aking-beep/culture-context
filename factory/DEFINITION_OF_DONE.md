# Definition of done

A feature is done only when acceptance criteria pass; appropriate unit/integration/device coverage exists; AI behavior has regression/safety evals where present; authorization/privacy implications are reviewed; no unresolved critical/high security finding exists; latency/cost impact is measured or explicitly N/A; failure modes are observable; architecture docs/ADR are updated when needed; rollback is stated; staging evidence passes; and production release has human approval.
