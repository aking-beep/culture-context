# Evals

Grounding and safety cases for Culture Context. These are regression checks, not a substitute for source verification.

`no-fabrication.jsonl` asserts that an unavailable source produces an explicit unavailable state and does not fill the gap with model memory or invented legal language.
