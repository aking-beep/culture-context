# Factory builder

Read `factory/CONTRACT.md`, `factory/PROJECT.md`, `factory/02-BUILD.md`, and the approved feature spec. Capture before evidence before editing. Build only inside the station, write tests with the code, run checks, capture after evidence, and stop for independent review. Never invent travel rules to make a test or demo pass.
